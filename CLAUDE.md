# data-converter — 项目约定（给 AI 协作时参考）

纯前端、单文件的数据格式互转工具。面向人类的说明见 `README.md`；本文件记录**改代码时必须知道的红线与约定**。

## 红线（不要破坏）

- **单文件、零构建、零依赖**：全部 HTML / CSS / JS 都在 `index.html` 一个文件里。**禁止**引入打包器（Vite/Webpack 等）、框架（React/Vue 等）、npm 依赖或构建步骤。新增能力用原生 JS 实现。
- **纯前端、无后端**：所有解析与转换在浏览器本地完成，不上传任何数据。「从 URL 导入」受浏览器 CORS 限制——目标接口必须允许跨域，否则无法读取。
- 主题用根元素 `html.dark` 类切换，配色全部走 `:root` / `html.dark` 里的 CSS 变量。新增颜色请复用已有变量，不要硬编码。
- localStorage 主题 key 固定为 `dc-theme`。

## 文件结构

- `index.html` — 全部代码（结构 + 样式 + 脚本），含 JSON/YAML/XML/CSV 解析与序列化。
- `favicon.svg` — 蓝紫渐变圆角方块 + 白色 `⇄` 标志。
- `.github/workflows/deploy.yml` — GitHub Pages 部署：仓库根即站点根，push 到 `main` 自动部署。
- `README.md` — 面向用户的功能说明、格式限制与在线访问地址。
- `CLAUDE.md` — 本文件，记录中间模型与有损转换约定。

## 中间模型与转换约定

工具采用「先解析为 JS 对象图，再序列化为目标格式」的中间模型。

### JSON

- 使用浏览器原生 `JSON.parse` / `JSON.stringify`。
- 解析失败时提取行列信息并展示。

### YAML

- 解析器与序列化器直接复用 yaml-viewer 的手写实现。
- 覆盖：块映射、块序列、嵌套、引号字符串、单层流式集合 `[a, b]` / `{k: v}`、块标量 `|` / `>`、行内注释、文档分隔符。
- 不支持：锚点 `&`、别名 `*`、标签 `!`、Tab 缩进、多文档（仅解析首个并提示）。

### XML

- 解析使用原生 `DOMParser`（`text/xml`）。
- 对象 → XML：
  - 根元素名称可由用户输入，默认 `root`。
  - 对象键名作为子标签名；非法 XML 名称字符替换为下划线。
  - 数组项统一使用 `<item>` 标签（简单且可预期）。
  - 基本类型直接作为文本节点输出；`null` / `undefined` 输出为空元素 `<null/>` 或 `<key/>`。
- XML → 对象：
  - 属性名前缀 `@_`，如 `@_id`。
  - 文本节点（含 CDATA）键名为 `#text`。
  - 仅含单个文本子元素且无属性时，直接返回字符串。
  - 同名元素合并为数组。
- **有损点**：数组还原为 `{item: [...]}`、数字/布尔/null 类型变为字符串、属性与纯文本的区分不可逆。

### CSV

- 解析器基于 RFC 4180，支持引号字段、`""` 转义、字段内逗号/换行。
- 分隔符自动检测：候选逗号、Tab、分号；统计首行之后（引号感知）出现频率取最高。
- CSV → 对象：首行为表头，后续每行映射为对象；空表头自动命名为「列N」。
- 对象 → CSV：
  - **仅支持对象数组**；数组元素必须是非数组对象，否则抛出「仅对象数组可转 CSV」。
  - 键并集作为表头，缺失字段输出空字符串。
  - 输出 RFC 4180 格式：逗号分隔，`\r\n` 行尾，含特殊字符的字段用双引号包裹。

## UI 约定

- **按钮文案保持极简**：每个按钮都带 SVG 图标，图标已承担表意，文字尽量短（如「导入」「粘贴」「下载」「示例」「转换」）。
- 工具栏布局：源格式分段控件、交换按钮、目标格式分段控件、「转换」主按钮、导入/粘贴/URL/示例、复制/下载、统计区。
- 双栏布局：左侧输入 `textarea`（等宽），右侧输出 `pre`（等宽只读）。
- 页头右侧两个图标按钮：GitHub 源码链接（仓库 `https://github.com/wangruofeng/data-converter`，新标签页打开）、深浅色主题切换。
- logo 方块 mark 文字为 `⇄`。

## 在线地址

已部署到 GitHub Pages：https://blog.wangruofeng007.com/data-converter/
